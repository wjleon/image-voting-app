# AI Image Model Comparison Voting App

A modern web application designed to blindly compare and vote on images generated by different AI models (e.g., Midjourney, DALL-E 3, Flux) for the same prompt.

## 🚀 Features

-   **Blind Voting**: Users see images without knowing which model generated them.
-   **Fairness Algorithm**: The system prioritizes showing images with the fewest impressions to ensure equal exposure.
-   **Internationalization (i18n)**: Full support for **English** and **Spanish**, including automatic prompt translation.
-   **Admin Dashboard**: A protected route (`/admin`) displays statistics on model performance (Win Rate, Total Votes, Impressions).
    -   **Secure Access**: Password-protected via HTTP Basic Auth.
-   **Internationalization (i18n)**: Full support for **English** and **Spanish**.
    -   **High-Quality Translations**: Prompts are translated using OpenAI (GPT-3.5/4) for accuracy.
-   **About Page**: Profile page with links to social media and blog.
-   **Responsive Design**: Built with Tailwind CSS for a seamless experience on mobile and desktop.

## 🛠️ Tech Stack

-   **Framework**: [Next.js 15](https://nextjs.org/) (App Router)
-   **Database**: PostgreSQL (via [Prisma ORM](https://www.prisma.io/)) - *Migrated from SQLite for Vercel*
-   **Styling**: Tailwind CSS
-   **Internationalization**: `next-intl`
-   **Translation**: `openai` (GPT-3.5 Turbo)
-   **Icons**: `lucide-react`

## 📦 Installation & Setup

1.  **Clone the repository**:
    ```bash
    git clone <repository-url>
    cd image-voting-app
    ```

2.  **Install dependencies**:
    ```bash
    npm install
    ```

3.  **Setup Database**:
    Initialize the SQLite database and generate the Prisma client.
    ```bash
    npx prisma db push
    ```

4.  **Ingest Data**:
    Scan the `images/` directory and populate the database with prompts and images.
    ```bash
    npx tsx scripts/ingest.ts
    ```

5.  **Run the App**:
    ```bash
    npm run dev
    ```
    Open [http://localhost:3000](http://localhost:3000) in your browser.

## 📂 Project Structure

-   `app/[locale]/`: Main application routes (Home, About).
-   `app/admin/`: Admin dashboard (not localized).
-   `app/api/`: Backend API routes (`vote`, `prompts`, `stats`).
-   `components/`: Reusable UI components (`Navigation`, `LanguageSwitcher`).
-   `prisma/`: Database schema and SQLite file.
-   `scripts/`: Utility scripts for data management.
-   `messages/`: Translation files (`en.json`, `es.json`).
-   `images/`: Source directory for prompts and images (outside `src` to avoid build bloat).

## 📜 Scripts

### Add New Images (Sync)
To ingest new images and automatically translate prompts (using OpenAI if configured):
```bash
npm run sync
```
This runs both the ingestion and translation scripts in one go.

### Check Translations
To verify that all prompts have valid Spanish translations:

### Check Translations
To verify that all prompts have valid Spanish translations:
```bash
npx tsx scripts/check-translations.ts
```

### Reset Votes
To clear all votes and impression counts (keeping images and prompts intact):
```bash
npx tsx scripts/reset-votes.ts
```

### Full Reset
To wipe the **entire database** (including images and prompts):
```bash
npx tsx scripts/reset.ts
```

## 🔗 Useful Links

-   **Home**: `http://localhost:3000`
-   **Admin Dashboard**: `http://localhost:3000/admin`
-   **About Me**: `http://localhost:3000/en/about`

## 👤 Author

**Wilmer Leon**
-   [LinkedIn](https://www.linkedin.com/in/wilmer-leon/)
-   [Medium Blog](https://medium.com/@wjleon)
